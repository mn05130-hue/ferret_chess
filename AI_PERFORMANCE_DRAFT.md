# AI 성능 개선 초안

현재 AI의 탐색 깊이 3과 평가 기준은 그대로 둔다. 최적화는 아래 순서로 별도 작업한다.

## 1. 기준 측정

- `chooseMove()`의 실행 시간, 방문 노드 수, 가지치기 횟수를 기록한다.
- 초반·중반·기물이 적은 종반 테스트 포지션을 고정한다.
- 데스크톱과 모바일에서 평균값보다 최악 지연 시간을 우선 확인한다.

## 2. UI 멈춤 제거

- `chess-ai.js`를 Web Worker에서 실행한다.
- 메인 화면은 보드 상태만 전달하고 AI가 선택한 수만 돌려받는다.
- 다시 시작하거나 타이틀로 이동하면 이전 요청을 무효화할 수 있도록 요청 ID를 사용한다.
- 현재의 650ms 연출 지연은 탐색 시간과 분리한다.

## 3. 중복 계산 축소

- 수 정렬 과정에서 같은 수의 점수를 반복 계산하지 않도록 한 번만 계산해 저장한다.
- 깊이 0에서 종료 판정에 필요한 작업과 평가용 합법 수 생성을 중복하지 않는다.
- 기동성 평가가 양쪽의 전체 합법 수를 매번 다시 만드는 비용을 측정하고 캐시 또는 간소화한다.

## 4. 시간 제한 탐색

- 반복 심화로 깊이 1부터 차례대로 탐색한다.
- 모바일 기준 시간 예산을 정하고, 시간이 끝나면 마지막으로 완료한 깊이의 최선 수를 사용한다.
- 따라서 복잡한 포지션에서도 응답 시간이 일정해지고 여유가 있는 기기에서는 더 깊게 볼 수 있다.

## 5. 추가 최적화

- 포지션 키 기반 전이표를 추가해 같은 위치의 재탐색을 줄인다.
- 캡처·승격·체크 수 우선 정렬을 유지하면서 필요하면 killer/history 휴리스틱을 검토한다.
- 효과가 측정된 항목만 남긴다.

## 완료 기준

- AI 탐색 중에도 화면 전환과 다시 시작 버튼이 즉시 반응한다.
- 취소된 AI 요청이 새 게임에 수를 적용하지 않는다.
- 기존 규칙 테스트와 고정 포지션의 선택 수 품질이 퇴행하지 않는다.
- 모바일의 최대 응답 시간이 정한 시간 예산을 크게 넘지 않는다.
